import { ClientMiddleware } from 'nice-grpc-common';
export declare type DeadlineOptions = {
    deadline?: Date | number;
};
export declare const deadlineMiddleware: ClientMiddleware<DeadlineOptions>;
