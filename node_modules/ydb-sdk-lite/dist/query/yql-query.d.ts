import { Grpc } from '../api';
import { IQueryParams } from './common';
export declare class YqlQuery {
    private grpc;
    private tablePathPrefix;
    constructor(grpc: Grpc, tablePathPrefix: string);
    execute(inputQuery: string, parameters?: IQueryParams): Promise<Record<string, unknown>[][]>;
}
//# sourceMappingURL=yql-query.d.ts.map