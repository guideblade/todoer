/**
 * Session pool.
 */
import { Grpc } from './api';
import { Session } from './session';
export declare class SessionPool {
    private grpc;
    private tablePathPrefix;
    private sessions;
    constructor(grpc: Grpc, tablePathPrefix: string);
    /**
     * Run function with session, allowing to execute prepared queries.
     */
    withSession<T>(fn: (session: Session) => T): Promise<T>;
    destroy(): Promise<void>;
    private createSession;
    private destroySession;
}
//# sourceMappingURL=session-pool.d.ts.map