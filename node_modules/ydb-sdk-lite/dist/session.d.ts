import { Grpc } from './api';
import { DataQuery } from './query/data-query';
export declare class Session {
    private grpc;
    private tablePathPrefix;
    sessionId: string;
    private busy;
    constructor(grpc: Grpc, tablePathPrefix: string);
    init(): Promise<void>;
    executeQuery(...args: Parameters<typeof Session.prototype.executeQueryUnsafe>): Promise<Record<string, unknown>[][]>;
    setBusy(value: boolean): void;
    isBusy(): boolean;
    destroy(): Promise<void>;
    protected executeQueryUnsafe(...args: Parameters<typeof DataQuery.prototype.execute>): Promise<Record<string, unknown>[][]>;
}
//# sourceMappingURL=session.d.ts.map