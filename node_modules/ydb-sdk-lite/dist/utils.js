"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.appendMessageToError = void 0;
function appendMessageToError(e, message) {
    if (!/^\s/.test(message))
        message = ` ${message}`;
    const newMessage = `${e.message}${message}`;
    e.stack = `${e.name}: ${newMessage}\n${(e.stack || '').split('\n').slice(1).join('\n')}`;
    // important to assign e.message AFTER e.stack (to avoid duplication in stack)
    e.message = newMessage;
    return e;
}
exports.appendMessageToError = appendMessageToError;
//# sourceMappingURL=utils.js.map